# RemoteShell 项目分析报告

## 项目概述

**RemoteShell** 是一个单文件 Python Web 终端模拟器，通过浏览器（主要是手机浏览器）远程操作 Windows `cmd.exe`。

**架构**：Python 服务端 + 嵌入式 HTML/xterm.js 前端，HTTP 登录 + WebSocket 终端通信。

---

## 一、Bug 清单

### 严重

1. **PTY 读取线程静默失败** (`web_terminal.py:255-263`)
   `read_pty` 线程中通过 `loop.call_soon_threadsafe` 创建异步任务发送数据。如果 `ws.send()` 失败（连接断开），异常会被 asyncio 静默吞掉，日志里没有任何提示。

2. **WebSocket 断开后 token 不清理** (`web_terminal.py:239-271`)
   `ws_handler` 的 `finally` 只调了 `proc.terminate()`，但没有从 `terminals` 字典中删除 token。token 永久有效，且字典持续增长（内存泄漏）。

3. **Token 永不失效，无会话超时**
   一旦登录拿到 token，可以永久使用。没有过期机制，也无法主动登出。

4. **无登录频率限制** (`web_terminal.py:224-237`)
   `/login` 端点没有速率限制，可被暴力破解。

### 中等

5. **PTY 读取线程可能残留** (`web_terminal.py:255-263`)
   `proc.terminate()` 调用后，`proc.read()` 不一定立即返回空数据（存在竞态窗口）。极端情况下线程会多存活几秒。

6. **fitAddon.fit() 初始调用时机问题** (`web_terminal.py:174`)
   终端打开后立即调用 `fit()`，但此时 DOM 布局可能尚未完全稳定（尤其在移动端 CSS 加载完成前），导致终端尺寸不准确。

7. **硬编码的公网 URL** (`web_terminal.py:276`)
   `print("Remote Shell Ready: http://home.coopez.cn:5000")` —— 无论实际运行在哪台机器上，都打印这个地址，容易误导。

8. **CDN 依赖无 fallback** (`web_terminal.py:24-26`)
   xterm.js 从 jsdelivr CDN 加载。CDN 不可用或断网时，前端完全无法使用。

### 轻微

9. **SimpleHTTPRequestHandler 默认日志污染** —— 每个请求都往 stderr 打 access log。

10. **移动端键盘弹起时 terminal resize 不够平滑** —— `visualViewport` 的 200ms debounce 太短，iOS 键盘动画约 300-400ms，会触发多次 resize。

---

## 二、如何使用

### 安装

```bash
pip install -r requirements.txt
```

依赖：

- `websockets>=10.0`：WebSocket 服务端
- `winpty>=0.5`：Windows 伪终端接口

### 启动

```bash
python web_terminal.py
```

启动两个服务：

- **HTTP :5000**：提供登录页面 + `/login` API
- **WebSocket :5001**：处理终端 I/O

### 使用流程

1. 浏览器打开 `http://<服务器IP>:5000`
2. 输入用户名 `admin`、密码 `123456` 登录
3. 登录后自动连接 WebSocket，服务端启动 `cmd.exe` 伪终端
4. 在浏览器中操作命令行

### 手机端特性

- 宽度 ≤768px 时显示底部工具栏
- 工具栏提供：Ctrl+C、Ctrl+Z、Tab、Esc、Enter、Clear
- 支持 `safe-area-inset`（刘海屏适配）
- `visualViewport` 监听处理软键盘弹起/收起

---

## 三、产品改进建议

### 安全（最优先）

| 问题 | 建议 |
|------|------|
| 硬编码账号密码 | 支持环境变量 `RS_USER` / `RS_PASS` 或配置文件 |
| 纯 HTTP 明文传输 | 支持 TLS/HTTPS + WSS |
| Token 永久有效 | 24 小时过期 + 断开连接自动失效 |
| 无暴力破解防护 | 登录失败 5 次后 IP 锁定 |
| 无 logout | 增加登出按钮，主动销毁 token |

### 功能增强

- **可配置 Shell**：目前写死 `cmd.exe`，应支持 `powershell.exe`、`pwsh.exe`、WSL 等
- **端口可配置**：通过命令行参数或环境变量指定
- **多会话支持**：允许多客户端同时连接、独立终端
- **断线重连**：WebSocket 断开后自动重连，保留会话
- **窗口同步**：前端 resize 后通过 WebSocket 通知服务端同步 PTY 尺寸
- **剪贴板集成**：工具栏增加"粘贴剪贴板"按钮

### 移动端 UX

- 工具栏增加方向键、`|`、`>` 等常用符号
- 长按终端区域弹出上下文菜单（复制、粘贴、选择）
- 横屏自动全屏优化
- 连接状态指示器（已连接/断开/重连中）

### 工程化

- 前后端分离（HTML 模板独立文件）
- logging 模块替代 print
- Docker 化部署
- 终端操作审计日志

---

## 优先级建议

1. **P0 安全修复**：凭据可配置、token 过期清理、HTTPS/WSS 支持
2. **P1 核心体验**：断线重连、Shell 可配置、PTY 尺寸同步
3. **P2 移动优化**：剪贴板、工具栏增强、连接状态提示
4. **P3 工程完善**：日志、Docker、前后端分离
